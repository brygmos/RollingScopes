import React from 'react';
import { FC } from 'react';
import SearchBar from './SearchBar';

const AppRouter: FC = () => {
  return (
    <>
      <SearchBar finder={''} />
    </>
  );
};

export default AppRouter;
